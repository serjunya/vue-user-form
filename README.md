# Требования
- Node.js
- bash-подобный терминал

# Запуск
- В директории `ts-server`:
    ```bash
    $ npm i
    $ npm run start
    ```
- В директории `vue-client`:
    ```bash
    $ npm i
    $ npm run start
    ```